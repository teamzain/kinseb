/** @type {import('next').NextConfig} */
const nextConfig = {
    // Place this at the top level, NOT under "experimental"
    allowedDevOrigins: ['http://192.168.192.213:3000'],
  };
  
  module.exports = nextConfig;
  